{/* headerArray is an array of strings that will be used as the header of the table */}
{/* columnKeys is an array of strings that will be used as the keys to access the data in the database */}  
{/* interactiveElements is an array interactive elements that may be in the table e.g. buttons, links etc*/}
{/* database is a link to the appropriate database that the table will get data from.*/}
function Table(headerArray, columnKeys, interactiveElements, database)
{
    return(
        <>

        </>
    )
}

export default Table;